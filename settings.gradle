pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //github botbar
        maven { url 'https://www.jitpack.io' }
    }
}
rootProject.name = "CryptoCA"
include ':app'
